<Window x:Class="CsvIntegratorApp.RouteEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Editor de Rota" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="WaypointsGrid" Grid.Row="0" AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Endereço" Binding="{Binding Address}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="AddAddressButton" Content="Adicionar Endereço" Click="AddAddressButton_Click" Margin="0,0,5,0"/>
            <Button x:Name="RemoveAddressButton" Content="Remover Endereço" Click="RemoveAddressButton_Click" Margin="0,0,10,0"/>
            <ProgressBar x:Name="ProgressBar" Width="150" Height="20" IsIndeterminate="True" Visibility="Collapsed" Margin="0,0,10,0"/>
            <Button x:Name="SaveButton" Content="Salvar e Recalcular" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
