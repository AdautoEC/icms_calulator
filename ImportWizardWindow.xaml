<Window x:Class="CsvIntegratorApp.ImportWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Assistente de Importação e Roteirização — ICMS Integrator"
        Height="768" Width="1280"
        MinWidth="640" MinHeight="480"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Painel de Integração de Documentos Fiscais" FontSize="20" FontWeight="Bold" Margin="8,4,8,12" Foreground="#1E1E1E"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="380" MinWidth="320" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="4" Margin="8,0,8,0" BorderBrush="#DDD" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16">

                        <GroupBox Header="1. Selecionar Arquivos" FontWeight="SemiBold" FontSize="14">
                            <StackPanel Margin="8">
                                <Grid Margin="0,4,0,4">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="NFe (XML):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBox x:Name="NfePath" Grid.Column="1" VerticalAlignment="Center" IsReadOnly="True" Background="#F5F5F5"/>
                                    <Button Content="Procurar..." Click="PickNfe_Click" Grid.Column="2" Margin="8,0,0,0" MinWidth="90"/>
                                </Grid>
                                <Grid Margin="0,4,0,4">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="MDFe (XML):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBox x:Name="MdfePath" Grid.Column="1" VerticalAlignment="Center" IsReadOnly="True" Background="#F5F5F5"/>
                                    <Button Content="Procurar..." Click="PickMdfe_Click" Grid.Column="2" Margin="8,0,0,0" MinWidth="90"/>
                                </Grid>
                                <Grid Margin="0,4,0,4">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Text="SPED (TXT):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBox x:Name="TxtPath" Grid.Column="1" VerticalAlignment="Center" IsReadOnly="True" Background="#F5F5F5"/>
                                    <Button Content="Procurar..." Click="PickTxt_Click" Grid.Column="2" Margin="8,0,0,0" MinWidth="90"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="2. Processar e Calcular" FontWeight="SemiBold" FontSize="14" Margin="0,16,0,0">
                            <Button x:Name="PreFillButton" Click="PreFill_Click" Style="{StaticResource AccentButtonStyle}" Margin="8" Height="44" ToolTip="Inicia a leitura, correlação e cálculo de rotas dos arquivos selecionados.">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE895;" FontFamily="Segoe MDL2 Assets" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBlock Text="Processar e Calcular Rota" VerticalAlignment="Center" Margin="8,0,0,0" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Button>
                        </GroupBox>

                        <GroupBox Header="Status do Processamento" FontWeight="SemiBold" FontSize="14" Margin="0,16,0,0">
                            <StackPanel Margin="8">
                                <TextBlock x:Name="StatusText" Text="Aguardando início..." TextWrapping="Wrap" MinHeight="32"/>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <ProgressBar x:Name="LoadingIndicator" Height="12" IsIndeterminate="False" Minimum="0" Maximum="100" Visibility="Collapsed"/>
                                    <TextBlock x:Name="ProgressPercentageText" Grid.Column="1" Text="0%" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="Collapsed"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Ferramentas" FontWeight="SemiBold" FontSize="14" Margin="0,16,0,0">
                            <WrapPanel Margin="8">
                                <Button x:Name="ImportJsonButton" Content="Importar JSON de Cálculo" Click="ImportJson_Click" Margin="0,0,8,8"/>
                                <Button x:Name="SaveJsonButton" Content="Salvar JSON de Cálculo" Click="SaveJson_Click" Margin="0,0,8,8"/>
                                <Button x:Name="OpenVehicleEditorButton" Content="Gerenciar Veículos" Click="OpenVehicleEditor_Click" Margin="0,0,8,8"/>
                                <Button x:Name="ViewLogButton" Content="Ver Log de Cálculo" Click="ViewLog_Click" IsEnabled="False"/>
                            </WrapPanel>
                        </GroupBox>

                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="4" Margin="8,0,8,0" BorderBrush="#DDD" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Resultados do Processamento" Grid.Row="0" FontSize="14" FontWeight="SemiBold" Margin="16,16,16,8"/>
                    <DataGrid x:Name="PreviewGrid" Grid.Row="1" Margin="16,8,16,16" AutoGenerateColumns="False" 
                              IsReadOnly="False" CanUserAddRows="False"
                              ItemsSource="{Binding}" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsComplete}" Value="False">
                                        <Setter Property="Background" Value="#FAAAAA"/>
                                        <Setter Property="BorderBrush" Value="Red"/>
                                        <Setter Property="BorderThickness" Value="0.5"/>
                                        <Setter Property="ToolTip" Value="Esta linha possui informações incompletas."/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Modelo" Binding="{Binding Modelo, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding Modelo_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding Tipo_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Renavam" Binding="{Binding Renavam, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding Renavam_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Placa" Binding="{Binding Placa, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding Placa_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="MDF-e" Binding="{Binding MdfeNumero, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding MdfeNumero_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Data" Binding="{Binding Data, StringFormat='d', UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding Data_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Distância (KM)" Binding="{Binding DistanciaPercorridaKm, StringFormat='F0', UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding DistanciaPercorridaKm_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="N° NF-e Carga" Binding="{Binding NFeCargaNumero, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding NFeCargaNumero_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Emissão Carga" Binding="{Binding DataEmissaoCarga, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding DataEmissaoCarga_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Qtd (LT)" Binding="{Binding QuantidadeLitros, StringFormat='N4', UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding QuantidadeLitros_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Qtd Estimada (LT)" Binding="{Binding QuantidadeEstimadaLitros, StringFormat='N4', UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding QuantidadeEstimadaLitros_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Combustível" Binding="{Binding EspecieCombustivel, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding EspecieCombustivel_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Valor Unit." Binding="{Binding ValorUnitario, StringFormat='N4', UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding ValorUnitario_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Valor Total" Binding="{Binding ValorTotalCombustivel, StringFormat='N2', UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding ValorTotalCombustivel_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Alíquota Aplicada" Binding="{Binding AliquotaCredito, StringFormat=P0}"/>
                            <DataGridTextColumn Header="Crédito" Binding="{Binding ValorCredito, StringFormat='N2', UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding ValorCredito_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="N° NF-e" Binding="{Binding NFeNumero, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding NFeNumero_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Data Emissão" Binding="{Binding DataEmissao, StringFormat='d', UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding DataEmissao_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="Vínculo" Binding="{Binding Vinculo, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridTextColumn.CellStyle><Style TargetType="DataGridCell"><Style.Triggers><DataTrigger Binding="{Binding Vinculo_IsEdited}" Value="True"><Setter Property="Foreground" Value="#005A9E"/><Setter Property="FontWeight" Value="Bold"/></DataTrigger></Style.Triggers></Style></DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTemplateColumn Header="Ações">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="Ver Mapa" Click="ViewMap_Click" CommandParameter="{Binding MapPath}" Margin="2"/>
                                            <Button Content="Ajustar Rota" Click="AdjustRoute_Click" CommandParameter="{Binding}" Margin="2"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="4" Margin="8,12,8,0" BorderBrush="#DDD" BorderThickness="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="16">
                <Button x:Name="ExportConferenciaButton" Content="Exportar Conferência" Click="ExportConferencia_Click" Margin="0,0,8,0" MinWidth="160" Height="32"/>
                <Button x:Name="ExportExcelButton" Content="Exportar Excel" Click="ExportExcel_Click" MinWidth="140" Height="32" Style="{StaticResource AccentButtonStyle}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
